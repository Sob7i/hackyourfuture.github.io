/**
 * Retrieves a CV and motivation letter from the request body using `multer` (see
 * https://github.com/expressjs/multer), stores the submitted data and sends emails
 * to the applicant and HYF.
 *
 * @param {object} req
 * @param {object} req.body
 * @param {string} req.body.email applicant's email
 * @param {object[]} req.files array of files (CV and motivation letter)
 *
 * @param {object} res
 */
module.exports = (req, res) => {

};
